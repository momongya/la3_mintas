<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">みんタス</a>
    <button class="btn" type="button" data-bs-toggle="offcanvas" data-bs-target="#offcanvasRight" aria-controls="offcanvasRight">
      <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="offcanvas offcanvas-end" tabindex="-1" id="offcanvasRight" aria-labelledby="offcanvasRightLabel">
  		<div class="offcanvas-header">
    		<h5 id="offcanvasRightLabel">新規グループ作成</h5>
    		<button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas" aria-label="Close"></button>
  		</div>
  		<div class="offcanvas-body">
    		<ul class="navbar-nav">
        		<% if !current_user.nil? %>
				  	<a class="nav-link" href="/"><img src="<%= current_user.top_img %>" alt="user_image" width="40" height="40"><p><%= current_user.name %></p></a>
					  <a class="nav-link" href="/group/select">グループ一覧</a>
				  	<a class="nav-link" href="/group/join">グループ参加</a>
				  	<a class="nav-link" href="/signout">ログアウト</a>
				<% else %>
					<a class="nav-link" href="/">ログイン</a>
				  	<a class="nav-link" href="/signup">新規ユーザー登録</a>
				<% end %>
      		</ul>
  		</div>
	</div>
  </div>
  </nav>

<section class="center-vertical">
  <h1 class="center-vertical">新規グループ作成</h1>
    <div class="login_button">
    <% if !@error_message.nil? %>
			<div class="alert alert-danger" role="alert">
			  <% @error_message.each do |error| %>
				  <%= error %><br>
				<% end %>
			</div>
		<% end %>
		<form action="/group/create" method="post">
		      <label for="group_name">グループ名(独自のものを入力してください)</label><br>
        	<input class="form-control" name="group_name" placeholder="作成するグループの名前を入力" required><br>
        	<!--<input class="form-control" name="color" placeholder="グループカラーを選択"><br>-->
        	<input type="color" name="color" list="color-list" value="#D3EBE8"> グループカラーを選択<br><br>
                <datalist id="color-list">
                    <option value="#C5D4EB">
                    <option value="#D3EBE8">
                    <option value="#FEF0F0">
                    <option value="#FDD9DB">
                    <option value="#EDCCDC">
                </datalist>
            <!--<input type="button" name="colorful" value="ON" onclick="btnClick(this)"> 色を選択-->
            <!--<div class="hidden">-->
            <!--    <div>button1</div>-->
            <!--    <div>button1</div>-->
            <!--</div>-->
        	<input class="form-control" type="submit" value="作成">
    	</form>
	</div>
</section>

<script>
  function btnClick(btn) {
    if (btn.value === 'ON') {
      btn.value = "OFF";
    } else {
      btn.value = "ON";
    }
  }
</script>